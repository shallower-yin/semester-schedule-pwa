import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.github.shalloweryin.semesterschedule",
  appName: "日程计划表",
  webDir: "dist",
  android: {
    backgroundColor: "#f7f8fc"
  }
};

export default config;
